# Hub Registry 科技感UI设计文档

## 1. 设计理念

### 整体风格
- **风格定位**：未来科技感、数据控制中心、赛博朋克元素
- **视觉特征**：深色背景、霓虹光效、玻璃态设计、动态交互
- **交互理念**：数据流动感、实时反馈、沉浸式体验

### 设计灵感来源
- 科幻电影控制台界面（如《银翼杀手》、《创：战纪》）
- 现代数据可视化大屏
- 科技产品官网设计风格

---

## 2. 色彩系统

### 主色调
| 颜色名称 | 色值 | 用途 |
|---------|------|------|
| 科技蓝 | `#00D4FF` | 主品牌色、按钮、高亮、边框发光 |
| 深空蓝 | `#0066FF` | 渐变、阴影、深度效果 |
| 霓虹青 | `#00FFE0` | 辅助强调、成功状态、数据流动 |

### 背景色系
| 颜色名称 | 色值 | 用途 |
|---------|------|------|
| 深邃黑 | `#0A0E1A` | 主背景色 |
| 暗夜蓝 | `#1A1F2E` | 卡片背景、面板 |
| 太空灰 | `#2A3042` | 边框、分割线 |

### 功能色
| 颜色名称 | 色值 | 用途 |
|---------|------|------|
| 成功绿 | `#00E676` | 健康状态、成功提示 |
| 警告橙 | `#FFAA00` | 警告状态 |
| 错误红 | `#FF1744` | 错误状态、危险操作 |
| 文字白 | `#FFFFFF` | 主要文字 |
| 文字灰 | `#8B9DC3` | 次要文字 |

---

## 3. 字体系统

### 字体选择
- **主要字体**：Inter / SF Pro Display（现代无衬线）
- **代码字体**：JetBrains Mono / Fira Code（等宽字体）
- **数字字体**：Roboto Mono（数据展示）

### 字号规范
| 元素 | 字号 | 字重 | 用途 |
|-----|------|-----|------|
| 页面标题 | 32px | 700 | Dashboard大标题 |
| 卡片标题 | 20px | 600 | 模块标题 |
| 数据数字 | 28px | 700 | 统计数字 |
| 正文 | 14px | 400 | 描述文字 |
| 标签 | 12px | 500 | 小标签、状态 |

---

## 4. 组件设计

### 4.1 顶部导航栏
```
┌─────────────────────────────────────────────────────────────┐
│ [◆Logo]  Hub Registry          [🔔] [👤 admin] [⚙️]      │
└─────────────────────────────────────────────────────────────┘
```

**样式细节：**
- 高度：64px
- 背景：半透明深色 + 底部发光边框
- Logo：带霓虹发光效果的SVG图标
- 右侧图标：悬浮时显示科技蓝光晕

### 4.2 左侧菜单栏
```
┌──────────┐
│ ■ Dashboard      │
│ ─────────────    │
│ ○ 命名空间       │
│ ○ 仓库管理       │
│ ○ 镜像复制       │
│ ○ 系统设置       │
└──────────┘
```

**样式细节：**
- 宽度：240px
- 背景：半透明深色
- 选中项：左侧发光条 + 文字高亮
- 悬浮效果：背景微亮 + 光效滑动

### 4.3 系统健康状态卡片

**设计要点：**
- 网格布局：3x2 或 2x3
- 每个卡片包含：
  - 服务图标
  - 服务名称
  - 状态指示灯（脉冲动画）
  - 状态文字

**卡片样式：**
- 背景：`rgba(26, 31, 46, 0.8)` + backdrop-blur
- 边框：1px solid `rgba(0, 212, 255, 0.2)`
- 悬浮：边框发光增强 + 轻微抬升
- 圆角：12px

**状态指示灯动画：**
```css
@keyframes pulse {
  0% { box-shadow: 0 0 0 0 rgba(0, 230, 118, 0.7); }
  70% { box-shadow: 0 0 0 10px rgba(0, 230, 118, 0); }
  100% { box-shadow: 0 0 0 0 rgba(0, 230, 118, 0); }
}
```

### 4.4 数据统计卡片

**设计要点：**
- 4个大卡片横向排列
- 每个卡片包含：
  - 图标（带渐变背景）
  - 数据数值（大字号）
  - 数据标签
  - 增长趋势（可选）

**卡片样式：**
- 背景：渐变深色
- 顶部：彩色渐变条（不同功能不同色）
- 数值：霓虹色发光效果

### 4.5 玻璃态卡片 (Glassmorphism)

```css
.glass-card {
  background: rgba(26, 31, 46, 0.6);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(0, 212, 255, 0.15);
  border-radius: 16px;
  box-shadow: 
    0 8px 32px rgba(0, 0, 0, 0.3),
    inset 0 1px 0 rgba(255, 255, 255, 0.05);
}
```

### 4.6 按钮设计

**主按钮：**
- 背景：线性渐变 `linear-gradient(135deg, #00D4FF, #0066FF)`
- 文字：白色
- 悬浮：光晕增强 + 轻微放大
- 点击：涟漪效果

**次按钮：**
- 背景：透明
- 边框：1px solid `#00D4FF`
- 文字：科技蓝
- 悬浮：背景填充发光

### 4.7 代码块设计

**样式：**
- 背景：`#0D1117`
- 边框：1px solid `#30363D`
- 左侧：彩色语法高亮条
- 字体：等宽字体
- 行号：显示（可选）

---

## 5. 动效设计

### 5.1 入场动画
- 卡片依次淡入 + 从下方滑入
- 延迟：每个卡片间隔 100ms
- 时长：500ms
- 缓动：`cubic-bezier(0.4, 0, 0.2, 1)`

### 5.2 悬浮效果
- 卡片抬升：`transform: translateY(-4px)`
- 发光增强：`box-shadow` 扩展
- 边框变亮：透明度从 0.2 到 0.5
- 时长：200ms

### 5.3 状态指示器脉冲
```css
@keyframes status-pulse {
  0%, 100% { 
    opacity: 1;
    box-shadow: 0 0 0 0 rgba(0, 230, 118, 0.4);
  }
  50% { 
    opacity: 0.8;
    box-shadow: 0 0 20px 4px rgba(0, 230, 118, 0.2);
  }
}
```

### 5.4 数据更新动画
- 数字变化：滚动效果
- 图表：平滑过渡

### 5.5 扫描线效果（装饰性）
```css
@keyframes scanline {
  0% { transform: translateY(-100%); }
  100% { transform: translateY(100vh); }
}
```

---

## 6. 响应式设计

### 桌面端 (≥1440px)
- 左侧菜单常驻
- 卡片网格 4列

### 平板端 (768px - 1439px)
- 左侧菜单可折叠
- 卡片网格 2列

### 移动端 (<768px)
- 汉堡菜单
- 卡片单列堆叠
- 简化图表

---

## 7. 特殊效果

### 7.1 背景网格纹理
```css
background-image: 
  linear-gradient(rgba(0, 212, 255, 0.03) 1px, transparent 1px),
  linear-gradient(90deg, rgba(0, 212, 255, 0.03) 1px, transparent 1px);
background-size: 50px 50px;
```

### 7.2 角落光效装饰
- 卡片四角：微光点
- 顶部：渐变光带

### 7.3 数据流动效果
- 连接线：流光动画
- 进度条：霓虹流动

---

## 8. 设计交付物

1. **UI设计稿**：Figma/Sketch文件
2. **组件库**：Storybook
3. **样式规范**：CSS变量系统
4. **动效规范**：Lottie/Framer Motion
5. **切图资源**：SVG图标、背景纹理

---

## 9. 技术实现建议

### CSS技术
- CSS Variables：主题色系统
- backdrop-filter：玻璃态效果
- box-shadow：多层发光效果
- gradient：渐变背景
- animation：复杂动效

### React技术
- styled-components / emotion：CSS-in-JS
- framer-motion：动画库
- recharts / echarts：图表库
- react-spring：物理动画

---

## 10. 设计检查清单

- [ ] 所有颜色符合无障碍标准（WCAG 2.1）
- [ ] 动效不会引起眩晕（prefers-reduced-motion）
- [ ] 深色模式对比度充足
- [ ] 响应式布局测试通过
- [ ] 性能优化（动画使用 transform/opacity）
- [ ] 图标统一风格
- [ ] 间距系统一致（8px基准）